#include <iostream>
using namespace std;

int main() {

	for (int i = 0; i <= 100; i++) { // for loop to count to 100
		// could use modulo to so when the index array is at a multiple of 3, 5, or both
		cout << i << endl; 
	}

	return 0;
}